import React from 'react';
import {motion} from 'framer-motion';
import {useRef, useEffect, useState} from 'react'

const Carousel = () => {
  return (
  <motion.h1 animate={{x: 250}}>POP</motion.h1>
  )
};

export default Carousel;
